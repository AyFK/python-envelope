[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "python-envelope"
version = "1.0.0"

[tool.setuptools]
package-dir = {"" = "src"}
py-modules = ["main"]
